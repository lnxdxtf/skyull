/// Code that will generate the template structure for the command new
mod codegen;
/// Command "new"
pub mod new;
/// Templates definitions
mod templates;
/// Add the dependencies for each template
mod add_dependencies;